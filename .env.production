# Carregado automaticamente em `vite build` (modo production).
# Sobrepõe `.env` local: evita MSW acidental quando o dev usa VITE_USE_MOCKS=true localmente.
# Se `VITE_AUTH_BACKEND=mock` estiver só no `.env`, o build ainda falha (ver vite.config.ts).
VITE_USE_MOCKS=false
